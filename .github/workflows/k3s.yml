name: Ansible - Install k3s

on:
  workflow_dispatch:
  push:
    branches:
      - main
env:
  GDALE_PRIVATE_KEY: ${{ secrets.GDALE_PRIVATE_KEY }}
  AZ_CLIENT_ID: ${{ secrets.AZ_CLIENT_ID }}
  AZ_SUB_ID: ${{ secrets.AZ_SUB_ID }}
  AZ_TENANT_ID: ${{ secrets.AZ_TENANT_ID }}
  AZ_SECRET: ${{ secrets.AZ_SECRET }}
jobs:
  manage_vm:
    name: Ansible - Install k3s
    strategy:
      matrix:
         folder: [k3s]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout codebase
        uses: actions/checkout@v4

      - name: Check runner
        run: |
          sudo apt-get update -y
          python3 --version
          pip3 --version

      - name: Set up SSH key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.GDALE_PRIVATE_KEY }}

      - name: List keys
        run: ssh-add -l

      - name: Install ansible
        run: pip install ansible[azure]

      - name: Check ansible version
        run: ansible --version

      - name: Install ansible collections
        run: ansible-galaxy collection install azure.azcollection
      
      - name: Find file
        run: find / iname requirements-azure.txt
        
      - name: Install Azure pre-req list
        run: pip install -r ~/.ansible/collections/ansible_collections/azure/azcollection/requirements-azure.txt

      # - name: Run playbook
      #   working-directory: k3s
      #   run: ansible-playbook install_k3s.yml -vvv #--private-key ~/.ssh/gdale-rancher-vm1



      # - name: Set up Python environment
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.10'

      # - run: mkdir ~/.ssh && touch ~/.ssh/gdale-rancher-vm1
      # - name: Add private key
      #   run: echo $GDALE_PRIVATE_KEY > ~/.ssh/gdale-rancher-vm1
      # - name: Set permissions on private key
      #   run: chmod 0600 ~/.ssh/gdale-rancher-vm1
      # - name: Install ansible
      #   run: pip install ansible[azure]
      # - name: Upload a single artifact
        # uses: actions/upload-artifact@v4
        # with:
        #   name: gdale-rancher-vm1
        #   path: ~/.ssh/gdale-rancher-vm1
      # - name: Check SSH key
      #   run: ssh-keygen -l -f ~/.ssh/gdale-rancher-vm1
      #   # run: eval $(ssh-agent -s) && ssh-add ~/.ssh/gdale-rancher-vm1

      # - name: Install Azure pre-req list
      #   run: pip install --upgrade -t /opt/hostedtoolcache/Python/3.10.13/x64/lib/python3.10/site-packages -r /opt/pipx/venvs/ansible-core/lib/python3.10/site-packages/ansible_collections/azure/azcollection/requirements-azure.txt

      # - name: Run playbook
      #   working-directory: k3s
      #   run: ansible-playbook install_k3s.yml -vvv --private-key ~/.ssh/gdale-rancher-vm1